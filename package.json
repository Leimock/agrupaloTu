{
  "name": "agrupalotu",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cypress:open": "cypress open"
  },
  "devDependencies": {
    "@vitest/coverage-c8": "^0.24.5",
    "cypress": "^11.1.0",
    "vite": "^3.2.0",
    "vitest": "^0.24.5"
  },
  "dependencies": {
    "prettier": "^2.7.1"
  }
}
